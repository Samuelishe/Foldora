# Project State

Дата: 2026-07-01.

GitHub README product landing pass: верх корневого `README.md` перестроен из инженерного state-документа в user-facing landing flow. README теперь начинается с короткого pitch, hero/screenshot placeholder без добавления assets, highlights, quick start для per-user install, example menu and screenshots placeholder. Технические details ниже сохранены и переставлены в порядок capabilities, requirements, install/uninstall, development run, dev publish, data locations, localization, limitations, safety/license/resources/docs. README по-прежнему не обещает MSI/MSIX, modern Windows 11 compact context menu, PNG-to-ICO conversion, pack import/export или exact right-click desktop placement; hero/mockup/screenshots остаются future branding/public-presentation work.

App icon folded ribbon refinement: предыдущая replacement-иконка технически была подключена, но manual visual review показал, что folded/origami ribbon выглядел как тонкая игла/крюк и недостаточно соответствовал выбранному reference direction. Текущий Foldora app icon заменён ещё раз: это self-authored folded blue/cyan folder mark на тёмном rounded app tile, с широкой light-cyan folded plane слева, более чистым folder silhouette и violet accent на front face. Source vector остаётся `src/Foldora.App/Assets/FoldoraIcon.svg`, generated multi-size Windows ICO остаётся `src/Foldora.App/Assets/Foldora.ico` with 16/32/48/256 px frames. `Foldora.App.csproj` продолжает задавать `ApplicationIcon`, а MainWindow, SettingsWindow и HelpWindow используют тот же icon resource. Внешние assets, icon packs, reference board image, README hero/mockup, full branding system, registry/MenuHost/install/shell behavior and settings JSON не менялись. MenuHost/CLI executable icons оставлены future/low priority, потому что MenuHost невидимый no-console host, а CLI developer-facing.

Settings runtime tab content centering fix: после предыдущего alignment follow-up пользователь проверил fresh Release binary и подтвердил, что Application, Help/About and Danger zone tab bodies всё ещё рендерятся как centered islands. Причина была не во внутренних forms/cards, а в shared `SettingsTabItemStyle`: `HorizontalContentAlignment`/`VerticalContentAlignment` использовались для центрирования header presenter и одновременно попадали в runtime selected tab content path. `SettingsTabItemStyle` теперь растягивает selected content, а header presenter центрируется напрямую внутри template. Wrapping content-sized tab header host и localized tab labels оставлены без изменений. Это XAML-only fix; ViewModel state, localization catalogs, settings JSON, registry/MenuHost/install/shell behavior не менялись.

Settings tab header clipping fix: ручная проверка после layout robustness pass показала, что Open/Copy и action buttons стали лучше, но верхний ряд вкладок SettingsWindow всё ещё выглядел сжатым и местами обрезал labels. Это оказалось отдельной проблемой TabControl header layout, не button template. `SettingsTabControlStyle` теперь использует wrapping content-sized header host для вкладок вместо стандартного `TabPanel`, а `SettingsTabItemStyle` получил компактнее padding/margin and explicit content alignment without fixed widths. Tab labels не переименовывались, SettingsWindow MinWidth не увеличивался, ViewModel/settings JSON/registry/MenuHost/install behavior не менялись.

Settings layout robustness pass: после ручной проверки вкладочного SettingsWindow исправлен clipping длинных localized button labels и слишком узкий resize state. Shared WPF button template теперь реально применяет `Padding` и content alignment, action/inline action buttons имеют более устойчивые min-size/padding без fixed `Width`, SettingsWindow minimum width поднят до практического минимума для RU/EN tab/action rows, Explorer actions остаются wrapping row, а Installation path rows сохраняют `*` path content + `Auto` actions. Это layout-only bugfix: ViewModel, settings JSON, registry/MenuHost/install/shell behavior and localization catalogs не менялись.

SettingsWindow tabbed layout cleanup: окно настроек больше не является длинной вертикальной простынёй секций. SettingsWindow использует category tabs `Application`, `Explorer menu`, `Installation`, `Help/About`, `Danger zone`, чтобы основная навигация не зависела от прокрутки. `Danger zone` изолирована в отдельной вкладке и не видна как содержимое при открытии настроек. Installation path rows стали компактнее: visible actions используют короткие `Open` / `Copy` labels (`Открыть` / `Копировать`), а command-host open action объясняет открытие containing folder через tooltip. Selected tab остаётся UI-only state WPF `TabControl` и не сохраняется в settings JSON. Registry/MenuHost/install/shell behavior, Core menu model and settings JSON semantics не менялись.

Visual polish pass v1: текущие WPF окна получили scoped визуальную доводку без изменения поведения. MainWindow стал более явно editor-focused за счёт спокойного page header, compact Explorer menu status chip, framed menu settings/menu entries sections, улучшенного empty state и отдельной save/discard footer surface. SettingsWindow получил более ровный section rhythm, status chip для Foldora Explorer menu, reusable path row containers для installed/user-data/MenuHost paths и менее кричащую danger zone. HelpWindow теперь визуально ближе к Settings/MainWindow: общий page header, одинаковые section surfaces, читаемые boxed step rows и fixed close footer. Shared design resources расширены page header/status/empty/path/help/footer styles и мягкими accent/danger surface tokens. Registry/MenuHost/install/shell behavior, settings JSON semantics, Core menu model, localization catalogs, app/window icon and README hero не менялись.

Settings/Explorer integration cleanup: MainWindow теперь является focused editor для menu title, groups, entries, icons, save/discard и editor status/errors. Большие Explorer integration controls, dry-run/register/unregister, technical details и danger reset перенесены в SettingsWindow. После small UX correctness cleanup на главном экране остаётся только компактный Explorer menu status; Settings открываются через gear в title bar. SettingsWindow содержит Application/language, Explorer menu, Installation/path information and Danger zone. Explorer actions в Settings выполняются immediately, а не staged вместе с language Save. Dirty draft policy сохранена: dry-run/register требуют save/discard menu changes; unregister остаётся допустимым при unsaved draft. Reset через Settings очищает saved entries, возвращает localized default-title mode, отключает Explorer integration и после закрытия SettingsWindow перезагружает MainWindow draft.

Documentation-first UI/UX audit pass: manual inspection after Settings cleanup found design debt now tracked in `docs/UI_AUDIT.md` and `docs/TECH_DEBT.md`. Confirmed issues: redundant Settings entry point on MainWindow, developer-facing `Unsaved changes: True/False` status, cramped button geometry, SettingsWindow scrollbar/content gutter, prototype-like visual polish gap and missing app/build/window icon. No production code changes were made in this audit pass. Next practical UI step is small UX correctness cleanup before broader visual polish or README hero work.

Small UX correctness cleanup: MainWindow no longer shows the duplicate content-area `Manage in Settings` action; the title-bar gear remains the settings entry point and the editor shows only compact Explorer menu status. The raw unsaved-changes boolean was replaced with localized user-facing `All changes saved` / `Unsaved changes` style text across all enabled catalogs. Shared action button geometry now has larger padding/min-height for long localized labels, and SettingsWindow scroll content has a right-side gutter. Registry/MenuHost/install behavior and settings JSON semantics did not change. Product-grade visual polish and app/build/window icon work remain future stages.

Settings clarity/help/path actions cleanup: SettingsWindow now uses explicit Foldora Explorer menu status wording (`Foldora Explorer menu: On/Off` style), shows the dry-run action as `Preview changes` / `Предпросмотр изменений`, adds self-authored `?` help affordances for Explorer menu and installation paths, and gives installed app/user data/MenuHost paths Open/Copy actions. These are App-layer UI actions only; shell registry behavior, MenuHost behavior, install scripts and settings JSON semantics did not change. A full Help/About/Instructions window remains future work.

Settings help/layout regression fix: follow-up manual inspection found that `?` help affordances looked like clickable buttons while only exposing hover tooltips, long help text did not wrap, and Settings inline action rows became too wide after the shared button geometry pass. Settings now uses passive self-authored `?` glyphs with wrapped tooltip text and a compact inline action button style for Explorer/path rows. This is WPF/XAML layout cleanup only; shell registry behavior, MenuHost behavior, install scripts and settings JSON semantics did not change.

Button padding and minimum window sizing follow-up: manual SettingsWindow inspection still found cramped inline button text and broken-looking narrow layouts. Settings inline action buttons now have more horizontal padding, SettingsWindow has a higher minimum width for Explorer/path rows, and MainWindow has a higher minimum width for the editor/card layout. Windows remain resizable, vertical scrolling is preserved, and title-bar/footer button behavior did not change.

Help/About/Instructions foundation: SettingsWindow now includes a small Help/About entry point. The new resizable HelpWindow uses the existing WPF design system, scrollable content and fixed close footer, and explains the basic Foldora workflow, `.ico` selection, Windows 11 legacy menu location, HKCU legacy context menu scope, MenuHost role, installed/user-data paths, uninstall behavior and 0BSD/resource policy notes. This is App-layer UI only: registry/MenuHost/install behavior and settings JSON semantics did not change. The window is not a full help center; screenshots, richer onboarding and full long-form translation review remain future polish.

Состояние: legacy context menu генерируется в HKCU под Foldora-owned roots, но видимый верхний пункт берётся из `FoldoraSettings.CreateFolderMenu.Title`. Текущий MVP-вид Explorer menu: `Создать папку -> optional one-level groups -> entries`, без лишнего видимого слоя `Foldora`; technical registry key остаётся `Foldora`. `FolderMenuEntry.GroupName` реализует простую одноуровневую группировку: пустое значение оставляет entry в root menu, непустое создаёт submenu с visible title группы. Entry/group registry key names остаются техническими; `GroupName` и `DisplayName` не используются как registry path. Entry keys получают registry `Icon = <entry.IconPath>`, если imported `.ico` существует. `foldora register-menu`, `foldora register-menu --dry-run`, `foldora register-menu --host-path`, legacy alias `--cli-path`, `foldora unregister-menu` и `foldora menu reset --yes` реализованы. Explorer registry commands должны запускать no-console `Foldora.MenuHost.exe`; `Foldora.Cli.exe` остаётся console tool для разработки и ручных команд. Команды `foldora apply --folder "<folder>" --icon "<absolute-icon-path>"`, `foldora apply --folder "<folder>" --entry-id "<entry-id>"`, `foldora create --target "<directory>" --entry-id "<entry-id>"` и `foldora clear --folder "<folder>"` работают через `desktop.ini`; команды `foldora menu list/add/remove/reset` управляют пользовательским списком меню. `menu add` поддерживает `--group "<group-name>"`, а `menu list` показывает group/root state. WPF editor реализует staged editing title/entries, add/remove draft entries, staged выбор `.ico`, прямой preview из `.ico` около 50x50 и controls для Explorer integration. UI cleanup phase 1 заменил технический `DataGrid` на карточки пунктов меню, скрывает `EntryId` из основного flow, разделяет обычную интеграцию и dangerous reset, а technical registry details показывает только в раскрываемом блоке. WPF grouping container UX показывает entries внутри visual containers `Без группы` и `<GroupName>`: non-empty group имеет editable header, inline delete-group confirmation, entry count, вложенные entry cards и contextual add-entry button. Entry cards теперь имеют compact view state и inline edit state; saved entries стартуют compact, новые draft entries стартуют раскрытыми, validation errors раскрывают affected card и показываются inline. `Без группы` не удаляется. `+ Добавить группу` создаёт первый draft entry в новой группе, но не вводит persistent empty groups или tree storage. Settings gear в custom title bar заменён на self-authored XAML vector icon без внешних ассетов. Импорт выбранных иконок в AppData происходит только по `Сохранить`; preview-файлы не генерируются. Если `ExplorerIntegrationEnabled = true`, WPF `Сохранить` после успешного settings save rebuild-ит Foldora-owned registry menu; если integration disabled, `Сохранить` пишет только settings.

Documentation consolidation: продуктовая концепция вынесена в `docs/PRODUCT_VISION.md`, целевой WPF/staged-save flow - в `docs/UX_FLOW.md`. Главный MVP-объект зафиксирован как пользовательский `FolderMenuEntry`; packs остаются будущим import/export механизмом.

MVP stabilization status: рабочий loop готов для ручной Windows 11 проверки по `docs/SMOKE_TEST.md`. Главное окно использует custom title bar на `WindowChrome`: settings gear, minimize, maximize/restore и close находятся в единой шапке окна, resize сохраняется, maximize должен respect Windows work area/taskbar без отдельного WinAPI layer. Content area больше не дублирует application title `Foldora`; вместо этого используется semantic page header `Меню папок`/`Folder menu`. Добавлено settings UI для языка приложения и persisted `Language` setting (`bg`, `cs`, `de`, `en`, `es`, `fr`, `hi`, `hu`, `id`, `it`, `ja`, `ko`, `nl`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `th`, `tr`, `uk`, `vi`, `zh-Hans`, `zh-Hant`) в `%AppData%\Foldora\settings.json`; WPF first-run выбирает matching complete locale для supported system UI culture и `en` для unsupported system languages, затем сохраняет выбор. Старые settings без language проходят такую же one-time detection, unsupported persisted values нормализуются и сохраняются как `en`; после этого язык системы не переопределяет ручной выбор. SettingsWindow является resizable, содержит scrollable content area, fixed footer actions, Application/language, Explorer menu, Installation and Danger zone sections. Localization foundation теперь использует embedded JSON catalogs `src/Foldora.App/Localization/*.json` для WPF labels/status/defaults across all enabled locales. `CreateFolderMenu.Title` получил `TitleIsCustom`: known complete-locale defaults без флага считаются untouched default, default UI показывает localized title, custom title не переводится. Новые draft entries используют localized display prefix/default folder name текущего UI language; существующие entries/groups/folder names не переводятся при смене языка. Validation cleanup перевёл WPF validation rendering на App-level catalog keys поверх Core invariant issue codes/parameters; Core `Message` остаётся compatibility/debug fallback для CLI и исключений. Оставшийся debt: Core compatibility defaults, CLI defaults/diagnostics/validation output, startup fatal dialog и external translation review. WPF design system foundation добавляет semantic resource dictionaries для palette/brushes, typography, spacing/radius/control sizes, buttons, inputs, cards/group containers and status banners; MainWindow и SettingsWindow используют эти ресурсы без изменения поведения. Action buttons `Primary`/`Secondary`/`Danger` используют общую геометрию через `ActionButtonStyle`. Repository hygiene now includes root `LICENSE` with 0BSD and `THIRD_PARTY_NOTICES.md`; current repository bundles no third-party visual assets. После bugfix startup-путь больше не делает sync-over-async загрузку settings до создания окна: `MainViewModel.CreateDefault()` создаёт сервисы без blocking `LoadAsync`, а фактическая загрузка settings остаётся в async `LoadAsync`. Startup exceptions теперь записываются в `%AppData%\Foldora\Logs\startup-error.log` и показываются через простой startup error dialog, чтобы окно не пропадало молча.

Stabilization pass 2026-07-01: README/LICENSE/THIRD_PARTY_NOTICES/resource-policy docs сверены с текущим MVP. README не обещает MSI/MSIX/stable public release flow, фиксирует 0BSD scope, отсутствие сторонних visual assets, legacy `Show more options` limitation, AppData layout, отсутствие PNG conversion и pack import/export. Compact/edit WPF code сверен как presentation-only: `IsEditing`, inline errors и group entry count не попадают в settings JSON/Core model; `EntryId` остаётся вне normal compact user flow, кроме tooltip. Документация уточняет manual smoke step для invalid inline errors и актуальную save-triggered rebuild policy.

Desktop.ini production default: `ReadOnlyFolderHiddenDesktopIni`, то есть folder `ReadOnly`, `desktop.ini` `Hidden`. Ручная проверка подтвердила `attrib` result `R/H`, сохранение custom icon после Explorer refresh/reopen и отсутствие System-related deletion warnings для новых Foldora folders. Repair/normalize command в MVP не реализован.

Technical debt foundation: `docs/TECH_DEBT.md` фиксирует active debt и accepted limitations. `TD-0001` повышен до high-priority research: текущая legacy context menu integration создаёт папку в правильной Desktop directory, но не получает original right-click cursor/icon-view coordinates через `%V`. Diagnostic prototype `diagnostics desktop-icon-position` вручную подтверждён: existing desktop icons можно двигать через Shell positioning, и Explorer grid displacement принят как acceptable behavior. После этого `Foldora.MenuHost` получил best-effort desktop placement для `create`: cursor screen position захватывается максимально рано, папка создаётся прежним Core flow, а если target directory равна user Desktop directory, созданный desktop item пытается reposition near captured cursor/menu selection point. После ручной проверки production path placement всё ещё мог не сработать, поэтому MenuHost теперь пишет JSONL diagnostics в `%AppData%\Foldora\Logs\menuhost-placement.log` и делает bounded retry только для race, когда Explorer desktop view ещё не видит newly-created item (`Desktop item was not found`). Placement failure non-fatal: успешное создание папки остаётся успешным. Подробный research snapshot находится в `docs/research/DESKTOP_ICON_PLACEMENT.md`. `TD-0002` переведён в `Cannot reproduce / Monitor`: текущая ручная проверка больше не воспроизводит first-created default icon, новые папки сразу показывают custom icon. Сейчас create/apply path не делает Shell refresh notification; random sleep, COM shell extension и exact original right-click positioning не добавляются без отдельного исследования.

Publish/dev layout foundation: `scripts/publish-dev.ps1` создаёт framework-dependent Release layout в `artifacts/publish/Foldora` и кладёт рядом стабильные executable paths:

```text
Foldora.App.exe
Foldora.Cli.exe
Foldora.MenuHost.exe
```

Script не регистрирует Explorer menu и не запускает приложение. При запуске WPF из publish-папки command-host resolver ищет sibling `Foldora.MenuHost.exe`; если host отсутствует, Explorer integration возвращает user-facing failure, а не регистрирует неправильный путь. CLI `register-menu --host-path` остаётся явным способом указать published `Foldora.MenuHost.exe`. `artifacts/` не входит в git.

Per-user install layout foundation: `scripts/install-user.ps1` переиспользует fresh dev publish output и копирует binaries в `%LocalAppData%\Programs\Foldora`, где `Foldora.App.exe`, `Foldora.Cli.exe` и `Foldora.MenuHost.exe` снова лежат рядом. При запуске installed WPF app resolver использует sibling `Foldora.MenuHost.exe`, поэтому Explorer registry command должен указывать на `%LocalAppData%\Programs\Foldora\Foldora.MenuHost.exe`. `Foldora.MenuHost.exe` не является сервисом, tray/background helper или autostart process; Explorer запускает его только по клику пользователя в Foldora legacy context menu. `scripts/uninstall-user.ps1` сначала пытается выполнить installed `Foldora.Cli.exe unregister-menu`, затем fallback-ом удаляет только Foldora-owned HKCU roots и удаляет install root. `%AppData%\Foldora` сохраняется по умолчанию, потому что settings/imported icons/logs являются пользовательскими данными, а существующие styled folders могут ссылаться на imported icons в `%AppData%\Foldora\icons`.

Следующий практический этап: ручная install smoke-проверка Explorer integration из `%LocalAppData%\Programs\Foldora` и best-effort desktop placement. Registry menu в install/manual проверке должен указывать на installed `Foldora.MenuHost.exe`, а не на Debug build output и не на console `Foldora.Cli.exe`.

Открытые вопросы:

- Финальное публичное имя продукта.
- Точный UX для выбора custom style.
- Возможен ли user-grade UX создания desktop folder под курсором без COM/modern shell integration.
- Нужен ли небольшой Shell refresh notification abstraction после create/apply для `TD-0002`.
- Фактическое поведение Explorer placeholders `%1` и `%V` на Windows 11 после повторной ручной проверки.
- Preview generation/cache policy, если прямой WPF preview из `.ico` окажется недостаточным.
- Full tree migration, drag-and-drop group ordering и group icons остаются future work; текущий MVP ограничен one-level `GroupName`.
- Manual release packaging beyond per-user install layout.
- User-facing diagnostics для failures внутри `Foldora.MenuHost.exe`, когда он запускается из Explorer.
